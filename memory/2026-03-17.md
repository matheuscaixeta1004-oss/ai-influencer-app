# 2026-03-17 — Notas Diárias

## Analytics Sync
- TikTok: 28.200 seguidores (+4%), 1.88M views, 8.88% engagement
- Instagram: 16.373 seguidores (+8.8%)
- Engagement em queda contínua pela 4ª medição (10.62→9.94→9.38→8.88%)

## Intel Diária
- Fanvue fechou Series A $22M, lançou AI Voice Calls
- Kling MC 3.0 rollout completo — múltiplas refs de rosto
- Perfis AI no Fanvue já monetizando com ~800 seg no IG
- Nicho AI influencer se comoditizando — diferencial = storytelling + persona

## AI Influencer App — Bug Fixes (deployed)
- Auth não persistia — sessão expirada não era detectada, dados não carregavam sem reset cookies
- Fix: Supabase client com persistSession, autoRefreshToken, detectSessionInUrl, storageKey
- Fix: AuthContext valida token expiry no init, força refresh se expirado
- Fix: signOut com try/catch, navigate sempre executa (botão sair funcionando)
- Fix: OAuth redirect vai pro /dashboard direto (não mais pra landing)
- Fix: Settings lê profile real em vez de valores hardcoded
- **Pushed to main, Vercel deploy triggered**

## AI Influencer App — Redesign Studio (em andamento)
- Matheus quer cards estilo Freepik Dreamachine (dark theme, controles inline)
- Image Generator Card: preview, prompt, qty, model, ratio, quality, refs laterais
- Video Generator Card: preview, prompt, qty, model, ratio, duration, resolution, sound FX, keyframes, refs
- Model Reference Card (novo): seleciona personagem como referência pra geração
- Studio persistence: salvar projetos no Supabase (múltiplos studios)
- Ícones: Material Symbols Rounded (zero emojis)
- Sub-agent spawned pra fazer o redesign

## GitHub SSH
- Chave SSH Ed25519 gerada e configurada no servidor
- Matheus adicionou no GitHub como "Gengis Khan VPS"
- Push funcionando via SSH (git@github.com)
- Branch limpa: só `main` no remote agora

## Pontos pra discutir (da intel)
1. Soft launch Fanvue agora vs esperar 100K TikTok?
2. Migração EvoLink v3 — go/no-go?
3. Persona de voz da Aiyara — brief?

## Studio — Sessão da Tarde (14:45-16:47 UTC)

### Persistência de Projetos (Supabase)
- Tabela `studio_projects` criada no Supabase (nodes JSONB, edges JSONB, viewport JSONB, name, timestamps)
- RLS policies configuradas (select/insert/update/delete por user_id)
- Trigger auto-update updated_at
- Migration SQL: `supabase/migrations/002_studio_projects.sql`
- CRUD lib: `src/lib/studio.ts` (listProjects, getProject, createProject, saveProject, renameProject, deleteProject)
- Auto-save debounced 2s usando refs (evita infinite loop)
- **Bug fix crítico**: loop infinito no auto-save — `triggerAutoSave` recriava callbacks que disparavam `rebuildNodes` em loop. Corrigido usando refs para todas dependências (zero deps no useCallback).
- `loadingProjectRef` bloqueia save durante carregamento inicial do projeto

### Tema Claro (Light Theme)
- Todos os cards migrados de #1e1e1e (escuro) pra #FFFFFF (branco)
- Token object `CARD` criado com: bg, border, shadow, divider, previewBg, pillBg, pillBorder, handleBg, handleBorder, inputBg, inputBorder
- ContextMenu migrado pra tema claro
- Model selector migrado pra tema claro
- Toolbar já estava claro (feito antes)
- Matheus confirmou visual correto via screenshot

### Aspect Ratio Adaptativo
- Preview area muda proporção com seleção (16:9 largo, 9:16 vertical, 1:1 quadrado, 4:5 levemente vertical)
- Card width adapta ao preview (min 320px, max 480px)
- Transição suave animada (transition-all duration-300)
- Helper function `getPreviewDimensions(ratio, maxWidth)`

### Seleção colada no card
- Removido padding wrapper (era `padding: 0 24px`)
- Handles usam posição negativa (left/right: -20) com overflow:visible
- Selection box-shadow movido de `.react-flow__node.selected` para `.studio-card`
- Classe `studio-card` adicionada em todos os cards

### Tela de Spaces (Freepik-style)
- View `SpacesView` como landing do Studio (grid de projetos)
- Cards com thumbnail (preview de nodes), nome, data
- Card dashed "+ Novo Space" para criar
- Busca quando 3+ projetos
- Rename inline, delete com confirm
- Seta ← no header pra voltar do canvas pro listing
- State `view: 'spaces' | 'canvas'` controla navegação

### Drag-to-Create (Handle → Espaço Vazio → Menu)
- `onConnectStart` salva pending connection (nodeId, handleId, handleType)
- `onConnectEnd` detecta se soltou no vazio (não em outro handle)
- Abre context menu na posição do drop
- Ao criar card via menu, auto-conecta ao handle de origem
- Mapeia handles por tipo de card (image_gen, video_gen, model_ref, legacy)

### Commits
- `bc71085` — light canvas, handle-buttons, multiple connections, light toolbar
- `f0fa6b9` — Studio project persistence (Supabase)
- `b05b1d2` — adaptive preview area + studio persistence
- `36aa7dc` — fix infinite loop in auto-save (refs)
- `803466c` — all Studio components → light theme
- `c53b79b` — selection border hugs card body
- `6f473a7` — Spaces view + drag-to-create + auto-connect

### Pendente
- [ ] Matheus pode estar vendo versão cacheada (dark) — pedido pra limpar cache/aba anônima
- [ ] Dropdown nativo do `<select>` herda dark mode do OS — precisa dropdown customizado
- [ ] Testar se auto-save funciona corretamente após fix do loop infinito
